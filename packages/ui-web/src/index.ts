/**
 * `@landit/ui-web` — the design system: tokens, primitives, icons, StickerBadge.
 *
 * T3 fills this in from `design-handoff/design/Land It.html`, where the CSS is
 * the spec. Fidelity is high: exact tokens, hard offset shadows, zero border
 * radius. Nothing here yet.
 */

/** Package identity. Exists so the scaffold has something real to import and test. */
export const UI_WEB_PACKAGE = '@landit/ui-web' as const;
